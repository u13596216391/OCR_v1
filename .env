# OCR 项目环境变量配置
# 请根据实际情况修改此文件

# ============ 前端配置 ============
# 前端API地址（用于访问后端服务）
VUE_APP_API_BASE_URL=http://10.4.34.108:8010/api

# ============ Label Studio 配置 ============
# Label Studio 服务地址（后端访问）
LABEL_STUDIO_URL=http://label-studio:8081

# Label Studio API Token（需要在Label Studio中生成）
# 生成方式：登录Label Studio -> Account & Settings -> Access Token
LABEL_STUDIO_API_TOKEN=

# Label Studio 默认项目ID（可选，也可在API调用时指定）
LABEL_STUDIO_PROJECT_ID=

# ============ GPU 配置 ============
# 是否启用GPU加速（true/false）
ENABLE_GPU=false

# 如果启用GPU，指定使用的GPU设备ID（多GPU时可用，例如：0,1）
CUDA_VISIBLE_DEVICES=0
